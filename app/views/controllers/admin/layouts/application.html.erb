<!DOCTYPE html>
<html>
  <head>
    <title>Admin - <%= content_for(:title) || "Lazcar" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="bg-gray-100">
    <nav class="bg-[#1A1E28] text-white shadow-lg">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <div class="flex-shrink-0 flex items-center">
              <%= link_to admin_root_path, class: "flex items-center gap-2" do %>
                <%= image_tag "lazcar.svg", class: "h-8 w-8" %>
                <span class="font-bold text-xl">Admin Panel</span>
              <% end %>
            </div>
            <div class="hidden sm:ml-6 sm:flex sm:space-x-8">
              <%= link_to "Dashboard", admin_root_path, class: "inline-flex items-center px-1 pt-1 border-b-2 border-transparent hover:border-white text-sm font-medium" %>
              <%= link_to "Cars", admin_cars_path, class: "inline-flex items-center px-1 pt-1 border-b-2 border-transparent hover:border-white text-sm font-medium" %>
              <%= link_to "Makes", admin_makes_path, class: "inline-flex items-center px-1 pt-1 border-b-2 border-transparent hover:border-white text-sm font-medium" %>
              <%= link_to "Models", admin_models_path, class: "inline-flex items-center px-1 pt-1 border-b-2 border-transparent hover:border-white text-sm font-medium" %>
            </div>
          </div>
          <div class="flex items-center">
            <span class="text-sm text-gray-300 mr-4"><%= current_user.email %></span>
            <%= link_to "Home", root_path, class: "text-sm hover:text-gray-300 mr-4" %>
            <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-sm hover:text-gray-300" %>
          </div>
        </div>
      </div>
    </nav>

    <div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4" role="alert">
          <p><%= notice %></p>
        </div>
      <% end %>

      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4" role="alert">
          <p><%= alert %></p>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>

